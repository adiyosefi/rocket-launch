import {shallow,mount} from "enzyme";
import React from "react";
import AppContainer from "./AppContainer";

describe('AppContainer', () => {
    // it('renders user search input correctly', () => {
    //     const wrapper = mount(<AppContainer />);
    //     wrapper.find("input[type='text']").simulate("change", { target: { value: "Falcon" }});
    //     expect(wrapper.find("input[type='text']").props().value).toEqual("Falcon");
    // });
});